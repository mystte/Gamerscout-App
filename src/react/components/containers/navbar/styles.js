import { colorNameToHex } from '../../../utils/color';

const styles = {
  container: {
    width: '100%',
    height: 70,
    backgroundColor: colorNameToHex('ebonyclay'),
  },

};

export default styles;