const styles = {
  container: {
    padding: 15,
    position: 'fixed',
    top: 70,
    right: 0,
    maxWidth: 520,
    minWidth: 400,
    width: '40%',
    zIndex: 10,
  },

  mbContainer: {
    padding: 15,
    position: 'fixed',
    top: 120,
    right: 0,
    maxWidth: 520,
    minWidth: 400,
    width: '40%',
    zIndex: 10,
  },

  notificationContainer: {
    marginBottom: 5,
  },
};

export default styles;
